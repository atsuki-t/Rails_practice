<p>
  <% if user_signed_in? %>
    <% if tweet.favorited_by?(current_user) %> <!-- ログインしているユーザーがファボしたかどうかで分岐 -->
      <span class="mr-2"><%=link_to "❤️ ", tweet_favorite_path(id: tweet.id, tweet_id: tweet.id), class: "text-decoration-none heart like-#{tweet.id}", method: :delete, remote: true %> <%= tweet.favorites.count %></span>
    <% else %>
      <span class="mr-2"><%=link_to "🤍 ", tweet_favorites_path(tweet.id), class: "text-decoration-none heart like-#{tweet.id}", method: :post, remote: true %> <%= tweet.favorites.count %></span>
    <% end %>
  <% else %>
    <span class="mr-2">🤍 <%= tweet.favorites.count %></span>
  <% end %>
  <span class="ml-4 mr-2"><%=link_to "💬 ", tweet_path(tweet.id), class: "text-decoration-none" %> <%= tweet.comments.count %></span>
</p>
