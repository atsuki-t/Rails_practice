<h2><%= link_to "← ", tweet_path(@tweet.id), class: "before_page" %>コメント作成</h2>

<%= form_for [@tweet, @comment] do |f| %>
  <p>
    <%= f.label :content,"内容" %>
    <%= f.text_field :content %>
  </p>
  <%= f.submit "コメントする" %>
<% end %>